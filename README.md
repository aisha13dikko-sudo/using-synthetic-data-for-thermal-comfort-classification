# Using Synthetic Data for Thermal Comfort Classification

UCL MSc AI for Sustainable Development project investigating whether **synthetic tabular/time-series data** can stand in for real sensor data when training thermal comfort classifiers — and where that substitution breaks down.

The project works with the [AutoTherm indoor dataset](https://huggingface.co/datasets/kopetri/AutoTherm) (physiological + environmental sensor readings labelled on a 7-point thermal comfort scale), generates synthetic versions of it with several different methods, and compares classifiers trained on real vs. synthetic data — including cold-conditions failure analysis and LLM-based classification.

## Project structure

The work is organised as a weekly log (`wk1` … `wk15`), each notebook a self-contained Colab experiment:

| Notebook | Focus |
|---|---|
| [wk1_autotherm_exploration.ipynb](wk1_autotherm_exploration.ipynb) | Baseline exploration of AutoTherm; identifies and fixes a label-leakage bug in the baseline feature set |
| [wk2_gaussian_copula.ipynb](wk2_gaussian_copula.ipynb) | Gaussian Copula synthetic data baseline (SDV) |
| [wk3_CTGAN_tvae_autotherm.ipynb](wk3_CTGAN_tvae_autotherm.ipynb) | CTGAN / TVAE synthetic data generation (SDV) |
| [wk4_mostlyai_autotherm.ipynb](wk4_mostlyai_autotherm.ipynb) | MostlyAI synthetic data generation |
| [wk5_forestdiffusion_autotherm.ipynb](wk5_forestdiffusion_autotherm.ipynb) | ForestDiffusion synthetic data generation |
| [wk6_paramodel_timeseries_autotherm.ipynb](wk6_paramodel_timeseries_autotherm.ipynb) | Parametric time-series modelling (DeepEcho) |
| [wk7_cold_failure_diagnostics.ipynb](wk7_cold_failure_diagnostics.ipynb) | Diagnosing why classifiers under-perform on cold-discomfort cases |
| [wk8_shared_utils.ipynb](wk8_shared_utils.ipynb) | Shared preprocessing/evaluation utilities reused by later notebooks |
| [wk9_llm_synthetic_generation.ipynb](wk9_llm_synthetic_generation.ipynb) | Synthetic row generation via an LLM (OpenAI API) |
| [wk10_mostlyai_verification.ipynb](wk10_mostlyai_verification.ipynb) | MostlyAI verification and conditional cold-case generation, with a provenance manifest |
| [wk_11_participant_audit.ipynb](wk_11_participant_audit.ipynb) | Per-participant audit of results |
| [wk12_costsensitive_ND_MOSTLYAI_VARIANCE.ipynb](wk12_costsensitive_ND_MOSTLYAI_VARIANCE.ipynb) | Cost-sensitive learning and MostlyAI run-to-run variance |
| [wk13_mostlyai_seeded.ipynb](wk13_mostlyai_seeded.ipynb) | Seeded MostlyAI reproducibility test |
| [wk14_llm_subjectwise.ipynb](wk14_llm_subjectwise.ipynb) | Static LLM classification on a subject-wise (leave-participants-out) split |
| [wk15_terra_multirun_2.ipynb](wk15_terra_multirun_2.ipynb) | Multi-run LLM classification, five traceable runs |
| [sdv_baseline_experiment.ipynb](sdv_baseline_experiment.ipynb) / [experiments/sdv_baseline_demo.ipynb](experiments/sdv_baseline_demo.ipynb) | Standalone SDV baseline demos |
| [temporal_llm_classification_clean.ipynb](temporal_llm_classification_clean.ipynb) | Cleaned-up temporal LLM classification pipeline |

Supporting folders:

- **`results/`** — CSV outputs and JSON **provenance manifests** (`wk*_manifest.json`) recording the exact run configuration (library versions, random seeds, train/test participant splits, label distributions) for each experiment, so results are traceable back to the notebook cell that produced them.
- **`trajectory_analysis/`** — plots of cold-discomfort sensor trajectories.
- **`llm_generation/`** — synthetic rows generated by an LLM, plus generation logs.
- **`llm_experiments/`** — zero-shot / few-shot LLM classification results (3-class and 7-class).

## Data

The [AutoTherm indoor dataset](https://huggingface.co/datasets/kopetri/AutoTherm) is streamed from the Hugging Face Hub at runtime via the `datasets` library (`load_dataset("kopetri/AutoTherm", "indoor")`) — no raw data is checked into this repository.

## Setup

The notebooks were developed and run in **Google Colab** (Python 3.12, CUDA runtime for the GPU-based generators). Each notebook installs its own extra dependencies in its first cell via `!pip install`, so notebooks can be opened directly in Colab without any local setup — click the "Open in Colab" badge at the top of a notebook.

To run any of the notebooks locally (e.g. in Jupyter) instead:

```bash
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

Notes:

- The `wk9`, `wk14`, `wk15`, and `temporal_llm_classification_clean` notebooks call the **OpenAI API** and expect an `OPENAI_API_KEY` — in Colab this is read via `google.colab.userdata`; locally, export it as an environment variable and adjust the key-loading cell accordingly.
- The `wk4`, `wk10`, `wk12`, `wk13` notebooks use the **MostlyAI** local SDK (`mostlyai[local]`) to train and sample synthetic data generators, which is compute-intensive and benefits from a GPU.
- `google-colab`-specific cells (`google.colab.userdata`, `google.colab.files`) only run inside Colab and will need to be swapped for local file I/O / env vars if running elsewhere.

## Reproducibility

Each major experiment writes a `wk*_manifest.json` to `results/`, capturing library versions, random seeds, and participant splits at the time of the run, so results can be traced back to the exact configuration that produced them (see e.g. [results/wk10_manifest.json](results/wk10_manifest.json)).

This provenance pattern was introduced starting with `wk10`. The `wk2`, `wk3`, and `wk4` notebooks call `.to_csv(...)` on their own results tables, but those files were written to the Colab runtime's local disk and were never downloaded or committed — they are not recoverable and do not exist in this repository. The numbers those notebooks report only exist as printed cell output inside the committed `.ipynb` files themselves.

## Data Availability

This statement follows a per-data-type disclosure: what is shared, where, and why anything is not re-hosted here.

- **Raw sensor data.** This project does not collect its own human-subject data. All sensor readings come from the third-party, public **AutoTherm indoor dataset** (Colley, Hartwig, Zeqiri, Ropinski & Rukzio, 2024, *AutoTherm: A Dataset and Benchmark for Thermal Comfort Estimation Indoors and in Vehicles*, Proc. ACM IMWUT 8(3), Article 96; also at [arXiv:2211.08257](https://arxiv.org/abs/2211.08257), DOI: [10.48550/arXiv.2211.08257](https://doi.org/10.48550/arXiv.2211.08257)), already de-identified by its original authors and permanently hosted at [huggingface.co/datasets/kopetri/AutoTherm](https://huggingface.co/datasets/kopetri/AutoTherm) under an MIT license. It is not duplicated into this repository — it is loaded directly from that persistent source at runtime (`load_dataset("kopetri/AutoTherm", "indoor")`) — because it already has its own stable, citable home and license; re-hosting a copy here would fork it from its authoritative version.
- **Derived data (this project's own outputs).** Every dataset this project generated — synthetic rows, per-run manifests, classification predictions, and evaluation tables — is committed in full under `results/`, `llm_generation/`, and `llm_experiments/`. Nothing here is "available upon request"; what exists is in the repository, and any gap is documented explicitly (see the note under [Reproducibility](#reproducibility) about `wk2`/`wk3`/`wk4`'s unrecovered intermediate CSVs).
- **Analysis code.** All code that goes from the raw AutoTherm data to every number reported in the thesis is in the notebooks listed under [Project structure](#project-structure) — there is no code held back. Software versions are pinned in [requirements.txt](requirements.txt); exact per-run versions and configuration (random seeds, train/test participant splits) are additionally recorded in `results/wk*_manifest.json` for each major experiment (see [Setup](#setup) and [Reproducibility](#reproducibility)).
- **Materials/stimuli.** The "materials" specific to this project are the LLM prompt templates used for synthetic data generation and classification. These are not a separate artefact — they are defined as code (`build_generation_prompt`, `build_temporal_prompt`, and equivalents) inside `wk9_llm_synthetic_generation.ipynb`, `wk14_llm_subjectwise.ipynb`, `wk15_terra_multirun_2.ipynb`, and `temporal_llm_classification_clean.ipynb`, and are versioned identically to the rest of the analysis code.
- **Codebook.** Not applicable — this is a quantitative machine-learning study with no qualitative coding scheme.
- **Preregistration.** Not applicable — this was exploratory/methodological comparison work (evaluating multiple synthetic-data generation methods against real-data baselines), not a preregistered confirmatory study.

## License

This project is licensed under the MIT License — see [LICENSE](LICENSE) for details.
